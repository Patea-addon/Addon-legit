# Minecraft / Fabric
minecraft_version=1.21.1
loader_version=0.16.9
loom_version=1.9-SNAPSHOT

# Fabric API (ajusta a la build exacta compatible con 1.21.1 si hace falta)
fabric_version=0.106.1+1.21.1

# Meteor Client (usa la version/tag que estes compilando en tu maquina,
# revisa el repo oficial de Meteor Client para la build exacta de 1.21.1)
meteor_version=1.21.1-SNAPSHOT

# Datos del addon
mod_version=1.0.0
maven_group=com.example.pvpaddon
archives_base_name=pvp-addon
